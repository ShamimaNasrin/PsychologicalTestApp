<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rimisfinalproject">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RimisFinalProject">
        <activity android:name=".DoctorC_Page"></activity>
        <activity android:name=".DoctorB_Page" />
        <activity android:name=".DoctorAPage" />
        <activity android:name=".DocLoginActivity" />
        <activity android:name=".DepressionDetailsActivity" />
        <activity android:name=".QuestionActivity" >

    </activity>
        <activity android:name=".DocListActivity" >

        </activity>
        <activity android:name=".ScoreActivity" >

    </activity>
        <activity android:name=".ThanksActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>